# Generated builds

The annotation builder writes immutable SQLite builds here. They are local scientific outputs and are intentionally ignored by Git. Generate one from a prepared cache with `scripts/build_annotations.sh`; do not commit the database, FASTA, RDS files, manifests, or machine-specific build metrics.
