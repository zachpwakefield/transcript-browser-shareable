# Critical review log

| Checkpoint | Severity | Finding and evidence | Resolution | Follow-up gate |
|---|---|---|---|---|
| Data audit | Critical | The processed exon RDS omits SP1-203, strips versions, and collapses repeated tags. | Raw GTF and FASTAs are authoritative; processed RDS is validation-only. | SP1 build must contain four exact versioned ENST records. |
| Reference audit | Critical | `annotation_cache` has no whole-genome FASTA, FAI/GZI, 2bit, or chromosome-size manifest. | Use the separately installed, checksum-pinned Ensembl 115 GRCh38.p14 FASTA/FAI and explicit aliases; fail startup when absent or changed. | Range, checksum, and primary-contig length tests. |
| Feature semantics | High | RDS `name` contains an intron-spanning genomic bounding interval. | Retain it only as audit text; draw only CDS-projected segment rows. | Junction-spanning regression fixture. |
| Identity | High | Feature tables use versionless transcript and peptide IDs. | Join against unique base IDs and persist the resolved local version; ambiguity fails. | Versioned/unversioned resolution tests. |
| Renderer | High | Independent DOM and Canvas height calculations would drift during expansion and resize. | One pure shared row-layout output controls rail height, Canvas y positions, hit bounds, and track height. | DPR 1/2 and resize checks. |
| Scope | High | A polished SP1-only screen could be mistaken for full v1. | Label the fixture as a technical preview and publish a machine-readable `scope` capability. | Manifest/UI wording review. |
| Renderer boundary | High | The planned igv.js custom-track boundary could not guarantee a single dynamic row-height, scroll, and hit-testing owner. | Exercise the plan's explicit fallback: use one shared layout model for accessible DOM controls and a local Canvas2D genomic viewport; omit remote igv.js registries and characterize screenshot/SVG as unsupported. | Shared-layout, gesture, DPR, resize, and browser interaction gates. |
| Full-build translation audit | Critical | A terminal selenocysteine (`U`) was compared with a terminal translated stop (`*`) before applying the equivalence rule, misclassifying an otherwise concordant mapping. | Apply selenocysteine equivalence at every protein position, including the terminus, and add explicit internal/terminal regression fixtures. | Full translation-status and exact-projection acceptance tests. |
| Build publication | High | Desktop FileProvider renamed an `O_EXCL` marker into a conflict copy, so a pathname marker alone could permit overlapping publishers. | Hold a kernel `flock` on a stable `/tmp` path keyed by resolved output root; keep private staging plus atomic rename and clean FileProvider artifacts. | Concurrent subprocess lock regression and final conflict-copy audit. |
| Runtime/API cross-review | High | Fresh review found release-lineage labels could be mutually consistent but wrong, intermediate symlink escapes, range precondition ambiguity, over-fetched region/export leakage, pagination false-empty states, and Unicode/whitespace search edge cases. | Enforce the exact v45/111/p14 identity, lexical containment at every component, RFC-style conditional range order, requested-region flags/bounds, over-fetch-aware emptiness, and normalized bounded search. | Package-unit and API regressions for every finding. |
| Frontend cross-review | High | The initial full UI could display the SP1 fixture before startup failure, report false empty states from over-fetch, request hidden broad-LOD features, show valid-empty text during feature errors, omit Table-tab feature demand, exceed the API span, and let modal focus escape. | Replace fixture fallback with a neutral gate; normalize server emptiness; scope selected overrides and lazy demand; separate loading/error/empty states; cap at 25 Mb; correct CDS-origin shading; trap and restore modal focus. | Frontend contract tests plus integrated browser/accessibility exercise. |
| Dense transcript window | Critical | Capping the transcript count did not bound the DOM/Canvas bitmap: a large expanded gene could still mount every retained row and allocate a DPR-2 backing store proportional to the full layout. | Preserve one complete shared layout but mount/draw only the visible row window plus overscan; cap the Canvas backing store at 16,000,000 pixels and 16,384 pixels per dimension, reducing only backing scale when necessary. | Windowing/unit tests plus ANK2 and PCBP1-AS1 browser observations; DPR 1 and resize remain external. |
| Detail request fan-out | High | Labeled/expanded display could request transcript details for rows far outside the viewport, making dense-gene cost follow total isoform count. | Derive transcript-detail demand from the viewport window and overscan plus explicit selected, pinned, and expanded context; retain bounded worker concurrency and abort stale demand. | ANK2 expanded run mounted 12 rows with a 13-request resource delta; repeat under other engines externally. |
| Inspector collection scale | High | The feature table and sequence viewer built DOM for all 500 features or all 1,821 lines of a 109,224-nt transcript. | Keep complete logical counts and full-copy semantics while virtualizing fixed-height sequence/table rows with accessible positions and table row indexes. | TTN-214 and OBSCN-212 scroll checks plus 40-test frontend suite. |
| Search re-focus | High | Preventing result-button `mousedown` blur could leave the DOM input focused while the internal palette state was closed; an immediate second click then produced no focus event and hid valid results. A delayed blur could also race re-focus. | Explicitly blur after result/form submission and cancel pending blur timers on focus/close. | Direct ENSP then immediate ENSE search/refocus browser regression. |
| Search scroll ownership | High | Gene or transcript search selected a row, but every lazy transcript-detail completion rebuilt layout and reran an unconditional selected-row visibility effect. User wheel movement was repeatedly forced back to the selected row, so dense genes could not be explored after search. | Replace the persistent selected-row invariant with a one-shot, gene-scoped reveal request. Consume it as soon as the intended row is present (including the already-visible case); later layout identities and row-height changes cannot mutate scroll position. Preserve fresh requests for repeated navigation, history restoration, feature selection, and disclosure. | Pure reveal-consumption regression plus ANK2 gene/transcript search browser checks under lazy loading; cross-engine scrolling remains external. |
| Exact gene-symbol submission | High | Transcript and protein search results intentionally carry their owning gene symbol for palette context. The generic exact matcher treated that contextual field as each result's own identity, so submitting a gene such as ANK2 could report many “exact” matches instead of opening the one exact gene. | Add a pure kind-aware resolver: one exact `gene.symbol` takes precedence, two or more distinct genes with that exact symbol remain ambiguous, and non-gene rows continue to resolve through their own exact stable identifiers or the bounded single-result fallback. Leave the search index, API schema, SQLite package, and annotation build unchanged. | Five focused search-resolution tests increased the frontend total to 112; the full 1.1.2 gate passed 169 tests plus TypeScript/build/offline/deterministic/startup/conflict checks. The installed native ANK2 workflow opened `ENSG00000145362` without the former warning, with zero console errors/warnings or external resources; native quit, active sync, and Release 1 integrity checks passed. |
| Transcript comparison order | High | Drag-only row ordering would be inaccessible by keyboard, fragile under vertical virtualization, and could let the DOM label rail diverge from Canvas track order. A custom order could also lose a comparison pair beyond the initial 120-row logical prefix. | Use native-button reorder menus with one-row and selected-anchor actions; keep canonical data immutable; apply one URL/session-backed permutation before filtering and shared layout; restore focus and announce the resulting visible position; retain selected neighbors as bounded context. | Helper, URL/session, filter, virtualization, native-control, typecheck, and production-build regressions; cross-engine interaction remains external. |
| PDF report export | High | A Canvas screenshot would not provide selectable transcript facts, exact sequence ranges, stable multi-transcript ordering, or a bounded reproducible report; an unbounded print path could also exhaust memory on dense genes. Cross-review additionally found that an empty source list could expand to every feature, base/versioned aliases could duplicate one transcript, a crafted locus could exceed its chromosome, and translated CDS length could hide a stored terminal stop codon. | Add an accessible **Save PDF** dialog and local `POST /api/v1/report/pdf` generator. Preserve the chosen subset in current custom order; offer summary, exon/CDS, protein-annotation, and exact-sequence sections; use vector structure models on a selected-union or current-locus scale; enforce exact 1-based inclusive labels and the 20-transcript/2,000-feature/20,000-total-character/10,000-per-excerpt/100-page/25-MiB bounds without silent truncation. Treat empty sources literally, reject duplicate resolved IDs and out-of-contig loci, and validate excerpts against authoritative stored-sequence lengths. Keep JSON/TSV machine-oriented. | Endpoint, pypdf text-extraction/exclusion, selection-order, canonical-ID, contig/range, content-type, page/size, and UI-dialog regressions. PDF remains untagged/non-PDF-UA, and standard-font glyph replacement is a documented limitation. |
| Native desktop launch | High | Finder-launched shell/Python children blocked while importing or opening multi-gigabyte files through Desktop/FileProvider, even though the same verified package started from Terminal. A naive embedded runtime also caused thousands of FileProvider copy/signing failures. | Package backend/site-packages/frontend/portable metadata as one signed runtime archive; install it once under Application Support; APFS-clone the immutable database and FASTA/FAI; materialize runtime-specific reference symlinks and identity receipts with unchanged pinned hashes; launch the base interpreter with a minimal environment and explicit private import paths. | App build/sign/install, exact manifest/capability, 206 FASTA range/ETag, and owned-process quit smoke passed. Fresh-environment replay remains external. |
| Local workspace trust boundary | High | Unvalidated `localStorage` could restore stale-build identifiers, oversized records, malformed arrays, or a prior view over an explicit deep link. Continuous scroll persistence could also create write churn and turn passive layout changes into navigation state. | Add schema-1/build-scoped parsing under `transcript-browser:workspace:v1`; cap the record at 512 KiB and every collection/field; accept a last view only after manifest/build validation; debounce ready-view writes; keep scrolling ephemeral; and give explicit URL state unconditional startup precedence. Corrupt/schema/build failure creates an empty current-build workspace. | The 102-test frontend suite passed parser, bound, corrupt/build-mismatch, explicit-URL, restore-disabled, session-v1/v2, and merge contracts. Chrome 150 SP1/ANK2 checks passed last-view restoration and explicit deep-link precedence without scroll capture. |
| User-authored scientific boundary | Critical | Notes or tags displayed beside GENCODE facts could be mistaken for source evidence, silently overwrite a colleague’s newer local interpretation during session import, or leak into scientific PDF reports. | Store `gene:<id>`/`transcript:<id>` annotations in a separate bounded browser-local map, label them explicitly as local user content, exclude them from SQLite/scientific facts/PDF, and require an explicit timestamp-aware session merge that reports additions, replacements, and preserved newer/equal local conflicts. | Annotation bounds, controlled editor/autosave/delete, portable-session validation/merge, comparison-export fields, and PDF exclusion passed the 102-test frontend and 25-test backend/API/PDF suites; the SP1 browser workflow passed note persistence/restoration. |
| Two-transcript comparison semantics | High | Reusing selection or pin state as an implicit comparison could reorder/scroll rows unexpectedly, lose the pair under filters/virtualization, or collapse genuine zero, missing, not-applicable, and not-loaded values. An unbounded comparison export could cross genes or execute user text as spreadsheet formulas. | Add a distinct gene-valid `comparisonTranscriptId` to URL/session state and bounded demand; retain it as explicit filter/window context without making it a scroll invariant; derive typed comparison cells; delegate pair placement to existing reorder helpers; and cap one-gene CSV/TSV at 20 visual-order rows with stable quoting and formula neutralization only for user-authored fields. | URL/session/demand/metric/export tests passed. On 129-transcript ANK2, `ENST00000682049` was placed at position 111 directly above the selected row at 112; CSV and TSV exported successfully while the live window stayed at 16 row controls and one Canvas. |
| Navigator, shortcut, and minimap scroll ownership | High | Dense genes need direct row navigation, but effects tied to current selection/layout could recreate the prior search-scroll lock. Global keys could fire while typing or in a modal, and a minimap could accidentally mount a second unbounded transcript tree. | Keep navigator/minimap helpers pure and callback-only; issue one consumable reveal for explicit selection; gate `/`, `J/K`, `P`, `C`, `Shift+C`, Page/Home/End outside editing/menu/modal contexts; derive a compact minimap from the shared row layout with only viewport and context markers; and allow scroll writes only for explicit pointer/keyboard gestures. | Shortcut, reveal, and geometry contracts passed. ANK2 gene-search scroll 2,600 and transcript `ENST00000514160` post-reveal manual scroll 1,800 remained stable; minimap End/Home/PageDown/click produced 7,619/0/934/3,784 with zero console entries. |
| Protein-feature expansion usability | High | The original disclosure helper behaved like an accordion: opening a second unpinned transcript silently collapsed the first. Fresh gene navigation also cleared expansion and selected automatic/labeled content, while translated disclosures were disabled outside Expanded mode. Users therefore could not keep several protein tracks visible and often saw no protein features by default. Asynchronous feature arrival could additionally change expanded lane count and row height. | Make disclosures additive and independently collapsible; cap the expansion set at 25 across interaction, URL, reconciliation, persistence, render context, and feature demand; keep translated disclosure buttons operable from other content modes; and make fresh gene navigation explicitly select the first translated transcript in Protein features mode without overriding explicit/restored state. Reserve expanded geometry from active feature sources before records arrive so background completion cannot move later rows, total height, minimap geometry, or scroll. | Five new frontend tests brought the frontend total to 107 and cover additive/forced-open behavior, overflow, translated defaults, multi-row ARIA disclosure, URL/workspace bounds, outside-prefix retention, demand bounds, and stable async layout. The full 1.1.1 gate passed 164 tests plus TypeScript/build/offline/deterministic/startup/conflict checks. |
| Quick PDF preset safety | High | Blindly replaying a prior PDF request on another gene/build could omit stale transcripts or reuse an invalid sequence range while appearing successful. | Persist only the last successful build-scoped scope/configuration; derive current selected/comparison/pinned rows in visual order; revalidate build, ownership, sections, sources, structure scope, and sequence range; and fall back to the full bounded dialog whenever the preset is incomplete or stale. The existing backend remains the only PDF generator. | Valid selected+comparison reuse, stale-build/ID, and sequence-range tests passed; the Chrome 150 workflow generated a selected-only Quick PDF through the existing bounded endpoint. |
| Application identity and diagnostics privacy | High | Calling both the interface release and scientific data “version 1.1” could imply an annotation change. Free-form diagnostics could expose usernames, paths, notes, searches, sequences, or external URLs. | Declare semantic application/launcher versions independently from the exact immutable annotation build hash; expose both separately; and build copied diagnostics from a bounded allow-list with loopback-origin normalization and path/user redaction, without reading user content or history. | Diagnostics/redaction tests passed with zero external resources. The prior installed interface was `1.1.1` build 3 with assets `index-DX_Ybgkz.js` (`8f260b9d…`) and `index-C4hHE25D.css` (`52865704…`). The installed current interface is `1.1.2` build 4 with `index-Bvswjowy.js` (`9676f29d…`) and the same CSS; its About/native smoke reported the exact separate identities and zero external resources. The annotation build remains unchanged. |
| Application 1.1 evidence boundary | Evidence boundary | Earlier Release 1, reorder, PDF, and search-scroll gates did not exercise the new persistence, comparison, notes, export, shortcut, Quick PDF, diagnostics, minimap, later multi-expansion behavior, or exact-gene-symbol resolution, so historical totals could not support completion. | Preserve the earlier evidence verbatim and run separate 1.1.0, 1.1.1, and 1.1.2 gates for focused/full automation and each patch-specific contract; record browser/native/sync/integrity observations only for the release that actually ran them. | The 1.1.0 gate passed 159 tests plus browser/native/sync/integrity evidence; the 1.1.1 patch gate passed 164 tests plus its recorded browser/native/sync evidence; the 1.1.2 gate passed 169 tests plus installed-app smoke, active sync, and Release 1 integrity evidence. Cross-engine, clean-environment, Safari, scientific, and unfamiliar-user gates remain open. |
| Frozen browser checkpoint | Evidence boundary | Final Chrome 150 / macOS / DPR-2 / 1280×720 workflow passed pan, ruler selection, search re-focus, overview, dense-row, long-sequence, and large-table interactions with zero external resources and zero console warnings/errors. The harness viewport override did not change the fixed viewport, and total-memory reporting was unsupported. | Record the warm metrics and bounded component-memory evidence without converting them into responsive, cross-engine, cold-cache, or total-memory claims. | Firefox, WebKit, Safari, DPR 1, real resize, cold-cache, and total-memory evidence remain open. |
| Human evidence | Blocking for scientific sign-off | Automated checks cannot supply biological interpretation approval, an actual Safari observation, or unfamiliar-scientist usability evidence. | Keep these as three explicit unchecked manual gates rather than converting them into automated claims. | Domain reviewer, desktop Safari, and section-16.4 task records. |

Open findings are added here with evidence and a blocking/non-blocking decision before dependent work proceeds.
