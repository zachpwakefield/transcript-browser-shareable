% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_differential_inclusion.R
\name{fit_sites_parallel}
\alias{fit_sites_parallel}
\title{Parallel Quasi-binomial GLM Fitting Across Sites}
\usage{
fit_sites_parallel(
  x,
  chunk_size = 2000L,
  progress = TRUE,
  verbose = TRUE,
  cooks_cutoff,
  BPPARAM = BiocParallel::SerialParam()
)
}
\arguments{
\item{x}{A `data.frame` or `data.table` with columns:
`site_id`, `condition`, `psi_adj`, `psi_raw`, and `total`.}

\item{chunk_size}{Integer. Approximate number of sites per chunk
to send to each worker (default 2000).}

\item{progress}{Logical. Show progress bar (default `TRUE`).}

\item{verbose}{Logical. Print progress messages (default `TRUE`).}

\item{cooks_cutoff}{Numeric or character. Cook's distance cutoff,
passed to [cutoff_num()].}

\item{BPPARAM}{A [BiocParallel::BiocParallelParam-class] object controlling
backend and worker settings. Default is [BiocParallel::SerialParam()].}
}
\value{
A `data.table` with one row per site and columns:
  `site_id`, `p.value`, `cooks_max`, `n`, `n_used`,
  `mean_psi_ctrl`, `mean_psi_case`, and `delta_psi`.
}
\description{
Fits quasi-binomial GLMs per splicing site (or event) in parallel,
splitting data into chunks to limit overhead. Each site-level fit
is performed by [`.site_glm()`].
}
\keyword{internal}
