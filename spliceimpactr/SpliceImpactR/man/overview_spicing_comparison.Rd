% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_multi_analysis.R
\name{overview_spicing_comparison}
\alias{overview_spicing_comparison}
\title{Overview of global splicing event distributions between conditions}
\usage{
overview_spicing_comparison(
  events,
  sample_df,
  depth_norm = c("exon_files", "user-given"),
  event_type = "AFE",
  conditions = c(control = "control", experimental = "case"),
  minReads = 10,
  output_file = NULL
)
}
\arguments{
\item{events}{`data.frame` or `data.table` of splicing events with at least:
`sample`, `condition`, `gene_id`, `inclusion_reads`, `exclusion_reads`, `psi`,
`inc`, `exc`.}

\item{sample_df}{Metadata `data.frame` with sample-level paths and conditions.}

\item{depth_norm}{Normalization mode: `'exon_files'` (compute from read files)
or `'user-given'` (use provided size factors).}

\item{event_type}{Character string specifying the event class (e.g., `"AFE"`, `"ALE"`).}

\item{conditions}{Named character vector mapping `control` and `experimental`
condition labels.}

\item{minReads}{Minimum reads required for inclusion or exclusion
(default = 10).}

\item{output_file}{Optional path to save a combined summary figure.}
}
\value{
Invisibly returns a combined [`patchwork`] plot object.
}
\description{
Generates a multi-panel comparison summarizing global splicing
characteristics (event counts, events-per-gene, PSI distributions)
between experimental and control conditions, normalized by sequencing depth.
}
\details{
The output combines:
- **Panel A:** Depth-normalized event counts per sample (Wilcoxon test)
- **Panel B:** Mean events per gene per sample (Wilcoxon test)
- **Panel D:** PSI cumulative distribution comparison (K-S test)

Size factors are computed internally using
[.get_size_factors_from_exons()], enabling robust normalization.
}
\examples{
ex <- load_example_data("sample_frame")
sample_frame <- ex$sample_frame
hit_index <- get_hitindex(sample_frame)
ov <- overview_spicing_comparison(hit_index, sample_frame, 'exon_files')
print(ov)
}
\seealso{
[.get_size_factors_from_exons()], [getSizeFactors()]
}
